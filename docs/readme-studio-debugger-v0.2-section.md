# README Studio Debugger v0.2 Section

This update adds a Studio Debugger v0.2 section to the project README.

## Purpose

The README is the first page most visitors see.

The new section makes it clear that Zero-CPU is not only a CLI virtual CPU. It
also has a visual debugger that explains instruction execution.

## Added README content

```text
- Studio Debugger v0.2 overview
- key debugger panels
- debugger_showcase.zasm demo file
- Studio demo flow
- CPU behavior covered by the demo
- link to docs/studio-debugger-v0.2.md
```

## Why this matters

This makes the project easier to understand from the GitHub front page.

A portfolio reviewer can quickly see:

```text
- custom ISA
- assembler and binary format
- virtual CPU
- visual datapath debugger
- BIO-OS direction
```
